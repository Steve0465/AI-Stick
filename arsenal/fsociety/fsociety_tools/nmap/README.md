# nmap

**What it is:** Network scanner to discover hosts, services, and OS fingerprints.

**How to use:**

Install: macOS via Homebrew `brew install nmap`. Run: `nmap -sV target`.

**Notes:**
- Use responsibly and legally.
- Some tools need dependencies (Ruby, Homebrew, Python).
